# 五仓库 README 结果新旧审计

审计时间：2026-09-10。目标是保证首页只把当前正式结果作为主要证据，同时保留必要的历史失败或基线用于追溯。

| 仓库 | README 当前正式来源 | 历史结果处理 | 结论 |
|---|---|---|---|
| Smoke | `verified-cpu-v5-annotated-20260906` | 没有旧目录引用 | 无需修改结果口径 |
| P0 | `verified-p0-cpu-v5-20260906` | v1/v2/v4 留在本地但 README 不引用 | 新增同一 JSON 派生的跨族汇总图 |
| P1 | `verified-p1-gpu-pretrained-10e-20260909` | CPU v2 仅保留文字追溯，不再嵌入旧图 | 修正底部口径为 GPU、10 epochs、640px、batch 1 |
| P2 | `trained-routing-analysis-20260909` | 随机初始化稳定性明确标成冷启动；移除被新 checkpoint 取代的旧 MOT 补充图 | README 首图改为训练后八层总览 |
| Final | `routing-ablation-20260909` | 阶段图与各正式来源按 SHA-256 对齐 | 以三条件三种子消融为唯一主结论 |

## P0 新图的必要性

P0 原有 contract coverage 图回答“哪些族通过统一 schema”，逐层 snapshot 图回答“每层的专家使用是多少”。新增跨族图回答第三个问题：“三族在同一次快照上的平均熵、负载 Gini 和主导专家占比是什么”。因此它有独立信息价值。

图中明确记录输入为 `000000000036.jpg`、CPU、seed 0、imgsz 320，并标注 MOE 与 MOT/LATENT 的 usage 语义不同。三族柱高不能解释成检测性能排名或专家质量排名。

## 自动完整性检查

本轮扫描五个 README 的本地图片引用：Smoke 1、P0 2、P1 4、P2 11、Final 1，共 19 个引用，缺失文件为 0。P2 新增的外观输入审计和绝对路由敏感性均来自当前正式训练结果；Final 报告保存的 P1 开销图与 P2 训练后证据分别和阶段仓库正式文件 SHA-256 一致。

历史结果没有删除。保留旧文件可以审计失败、CPU 调度噪声和开发过程，但 README 不再把它们与当前正式结论并列展示。
